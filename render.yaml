services:
  - type: web
    name: main-ocr-api
    env: docker
    plan: starter  # 1GB RAM minimum recommended for TrOCR small
    region: oregon
    dockerfilePath: ./Dockerfile
    autoDeploy: true
